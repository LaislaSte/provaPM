 <div class="container">
        <h1>Tela de Consulta</h1>
        <table class="table">
            <thead>
                <tr>
                    <th>Nome</th>
                    <th>Endereço</th>
                    <th>Ações</th>
                </tr>
            </thead>
            <tbody>
               {{#each agendamentos}}
                <tr>
                    <td>{{dataValues.nome}}</td>
                    <td>{{dataValues.endereco}}</td>
                    <td>
                        <a class="btn btn-primary" href="/editar/{{dataValues.id}}" >Editar</a>
                        <a class="btn btn-danger" href="/excluir/{{dataValues.id}}">Excluir</a>
                    </td>
                </tr>
                {{/each}}
            </tbody>
        </table>
    </div>